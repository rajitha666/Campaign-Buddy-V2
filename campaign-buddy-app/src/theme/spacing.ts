/**
 * Spacing scale (px). Keep to this scale rather than one-off numbers so
 * padding/margins stay visually consistent across screens — mirrors the
 * 4/8/12/16/... rhythm used throughout the HTML prototype.
 */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
};

export const radius = {
  sm: 8,
  md: 12,
  lg: 14,
  xl: 16,
  xxl: 18,
  pill: 999,
  sheet: 26, // bottom-sheet top corners
};
