# Base URL the frontend calls. Leave blank to use the Vite dev proxy above
# (recommended for local dev). Set to your real API origin for prod builds,
# e.g. https://api.campaignbuddy.app
VITE_API_BASE_URL=
