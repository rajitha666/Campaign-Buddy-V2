# OS cruft
.DS_Store
Thumbs.db

# Local editor / tooling config
.claude/
.vscode/
.idea/

# Archived source snapshots — git history is the archive; don't commit zips
*.zip
/Archive/

# local dev port overrides (see docker-compose.override.example.yml)
docker-compose.override.yml
